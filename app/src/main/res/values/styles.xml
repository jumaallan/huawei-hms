<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="fontFamily">@font/balooda_medium</item>
    </style>

    <style name="FullScreenTheme" parent="Theme.MaterialComponents.Light.NoActionBar" />

    <style name="cardHeader">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/balooda_bold</item>
    </style>

    <style name="cardDetails">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/colorDivider</item>
        <item name="fontFamily">@font/balooda_medium</item>
    </style>

</resources>
